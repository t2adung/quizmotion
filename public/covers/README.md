# Bìa sách (optional)

Đặt ảnh bìa sách vào đây (nên là ảnh dọc, tỉ lệ ~2:3, vd `600×900`).
Chọn khi tạo video bằng `--cover <tên-file>` hoặc trong CLI tương tác.
Bìa hiển thị ở **slide đầu (intro)** và nhỏ hơn ở slide kết.
`sample-cover.svg` chỉ là mẫu — cứ xoá khi thêm bìa của bạn.
